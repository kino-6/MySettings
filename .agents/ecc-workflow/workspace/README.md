# Local Workspace Journals

This directory is for local session journals and resume notes. Its contents are
ignored by git because they may include machine-specific or private context.

Promote only stable, reusable lessons into `../spec/`.
