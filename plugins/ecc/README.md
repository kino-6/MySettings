# plugins/ecc

This directory is the local plugin target referenced by `.agents/plugins/marketplace.json`.

The plugin does not duplicate skill content. Its manifest points Codex at the repository-owned `.agents/skills/` directory, which is the canonical vendored ECC skill surface for this dotfiles repo.
